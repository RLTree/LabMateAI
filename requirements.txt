# Core requirements
networkx==2.8.8
numpy>=1.23.5
prompt_toolkit>=3.0.0
pandas>=1.5.3
scikit-learn>=1.1.3
matplotlib>=3.6.2
flask>=2.2.2
requests>=2.28.1
scipy>=1.9.3
jinja2>=3.1.2
gunicorn>=20.1.0
psycopg2-binary>=2.9.0  # Switched from psycopg2 to psycopg2-binary for easier installation
python-dotenv>=0.19.1

# Testing dependencies
pytest>=7.4.0
pytest-mock>=3.11.1
unittest-xml-reporting>=3.0.4  # Added for better test reporting in CI/CD environments
pytest-cov>=4.0.0  # For measuring code coverage of your tests
pytest-asyncio>=0.20.3  # If asynchronous tests are included

# Linting and code style (optional, but recommended)
flake8==6.1.0

# Mocking dependencies
mock>=4.0.3  # Added for mock compatibility

# Pin setuptools to avoid psycopg2 issues
setuptools==58.0.4  # Added to avoid conflicts with psycopg2 setup